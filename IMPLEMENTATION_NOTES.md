# 구현 결정 기록

## 2026-08-18 독립 지지·저항선 지표 추가

- 사용자 요청에 따라 삭제됐던 종합 참고 분석이나 가치평가를 복원하지 않고, 가격 차트에 독립된 지지·저항선 지표만 추가했다.
- 최근 최대 100거래일의 수정주가 OHLC에서 5거래일 국소 고점·저점과 20·60·100거래일 및 전체 표본 극값을 수집하며, 고가·저가 누락 시 실제 종가를 사용한 사실을 화면에 표시한다.
- 후보 가격을 1.5% 이내에서 군집화하고 현재가와 최소 0.3% 떨어진 가까운 지지선·저항선을 각각 최대 2개 선택한다.
- 차트 점선과 함께 가격, 현재가 대비 거리, 반응 횟수, 표본 수, 일봉 기준일과 계산 가정을 텍스트로 표시한다.
- 20거래일 미만, 잘못된 현재가 또는 구분 가능한 수준이 없는 경우 임의의 선 대신 계산 불가 사유를 표시한다.
- 지지·저항선은 과거 가격 반응을 요약한 후행 참고 지표이며 미래 가격 예측이나 투자 권유로 표현하지 않는다.

## 2026-08-18 확률·범위 전망 개선

- 기존 단일 종목 종가 유사 구간 모델이 실제 종목 대부분에서 MAE와 방향 기준을 동시에 통과하지 못해 결과 전체가 비어 보이는 문제를 개선했다.
- 한국투자증권 기간별 시세의 `FID_ORG_ADJ_PRC`를 해당 API 명세의 수정주가 코드인 `0`으로 고정했다.
- 최소 180거래일에서 최근 5거래일 수익률의 시간 감쇠 10·90백분위를 계산해 방향 검증과 독립적인 현재가 적용 범위를 제공한다.
- 방향 모델은 수익률, 이동평균 이격도, 변동성, RSI, ATR, 거래량 표준점수, 갭과 일중 범위로 구성한 OHLCV 특징을 사용한다.
- 방향 결과는 상승·보합·하락 확률로 만들고 과거 빈도 기준 확률과의 다중 클래스 Brier Score를 시간순으로 비교한다.
- 방향 모델이 기준을 이기지 못하거나 OHLCV가 부족하면 확률을 숨기고 `방향성 중립`으로 표시하되 변동 범위와 범위 포함률은 유지한다.
- 생성형 AI, 외부 유료 모델, 확정 목표주가와 매수·매도 문구는 사용하지 않는다.

## 2026-08-18 과거 데이터 기반 통계 전망 초기 구현 (현재 대체됨)

- 사용자 요청으로 종합 참고 분석이나 가치평가와 분리된 `과거 데이터 기반 통계 전망` 실험 기능을 추가했다.
- 한국투자증권 기간별 시세는 수정주가 반영 값으로 요청하고, 한 호출의 개수 제한을 피하도록 120일 달력 구간으로 나눠 최근 약 3년을 수집한다.
- 도메인에는 전망 검증용 최대 800거래일을 보존하고 기존 차트에는 최근 100거래일만 전달한다.
- 특징은 5일 모멘텀, 20일 모멘텀과 20일 변동성이며 표준화 거리가 가까운 과거 구간 24개의 이후 5거래일 수익률 분포를 사용한다.
- 결과는 상승 비율, 수익률 중앙값·20백분위·80백분위와 이를 현재가에 적용한 가격 범위로 제한한다.
- 각 워크포워드 시점에서는 그 시점까지 결과가 확정된 표본만 학습 후보로 사용해 미래 데이터 누출을 막는다.
- 최소 180거래일과 검증 20회를 요구하며, 방향 일치율 52% 이상과 변화 없음 기준 대비 MAE 개선율 5% 이상을 모두 충족해야 전망 숫자를 표시한다.
- 검증에 실패하면 숫자를 숨기고 `예측력 확인 안 됨`과 실제 검증 지표를 표시한다.
- GPT나 외부 생성형 AI는 전망값 생성에 사용하지 않으며 결과를 목표주가·매수·매도 의견으로 표현하지 않는다.

## 2026-08-18 참고 분석 기능 제거

- 사용자 요청에 따라 종합 참고 분석, 단순 PER 참고가와 S-RIM 참고가를 화면·상태·도메인·데이터 매핑·문자열·테스트에서 제거했다.
- 종합 분석 카드에 포함됐던 기술지표, 지지·저항과 실적 규칙 점수도 함께 제거했다.
- 가치평가에만 필요했던 연간 EPS·ROE 입력 정규화와 지원 종목 분류를 제거했다.
- ETF, 우선주, 리츠, 금융회사와 EPS 비양수 종목도 별도의 평가 미지원 상태로 막지 않고 확보된 가격·차트·재무·DART 정보를 표시한다.
- 이 시점의 제품 범위는 종목 검색, 현재가, 최근 100거래일 차트, 핵심 재무정보, 최근 3년 실적, DART 공시 경로와 출처 안내였다.
- 아래 기록은 삭제 전 기능의 역사적 결정이며 현재 구현에는 적용되지 않는다.

## 2026-08-18 가치평가 입력 정규화·신뢰도 방어 (삭제 이전 기록)

- 특정 연도의 고점 EPS·ROE가 참고가를 과도하게 키우는 문제를 줄이기 위해 결산월이 같은 최근 최대 5개 연간 재무비율의 중앙값을 계산 입력으로 사용한다.
- 중복 재무기간은 한 번만 세며 결산월을 확인할 수 없으면 여러 기간을 연간 표본으로 간주하지 않고 최신값 하나만 사용한다.
- 비교 가능한 연간 표본이 3개 미만이면 개별 PER·S-RIM 결과는 가정 확인용으로 표시하지만 종합 참고가에서는 제외한다.
- 재무비율 EPS를 얻지 못해 현재가 응답의 EPS를 대신 사용한 경우에도 단일 표본으로 표시하고 종합 참고가에는 반영하지 않는다.
- 계산 EPS 기준 현재가 내재 PER과 사용 BPS 기준 현재가 내재 PBR을 함께 표시해 목표 배수와 현재 시장 배수를 비교할 수 있게 한다.
- 신뢰도 조건을 통과한 PER 기준값과 S-RIM 지속계수 0.9 참고가가 2배 이상 차이 나면 임의 평균을 만들지 않고 모델 괴리를 안내한다.
- 기본 목표 PER 10·15·20배와 S-RIM 요구수익률 10%는 기존 제품 가정을 유지하며, GPT가 계산값을 생성하거나 보정하지 않는다.

## 2026-08-14 가치평가·종목 검색 확장 (삭제 이전 기록)

- 사용자 요청으로 기존 6자리 종목코드 전용 입력을 종목명·종목코드 통합 검색으로 확장했다.
- 자동완성 원본은 OpenDART 상장사 고유번호 목록의 종목명과 6자리 종목코드를 사용한다.
- 기존 단순 PER 참고가에 S-RIM 참고가를 추가했다.
- S-RIM은 KIS의 BPS와 ROE를 기본 입력으로 사용하고, 평가 ROE와 요구수익률은 사용자가 수정할 수 있다.
- 초과이익 지속계수는 0.8, 0.9, 1.0 세 경우를 구분해 표시한다.
- 요구수익률 기본값은 10%이며 실시간 회사채 수익률로 오인되지 않도록 사용자 가정으로 표시한다.
- 화면과 코드에서는 계산 결과를 적정주가나 예측값으로 부르지 않고 PER 참고가 또는 S-RIM 참고가로 표현한다.
- 계산과 자동완성에는 GPT API를 사용하지 않는다.

## 2026-08-14 종합 참고 분석·종목 목록 프리로드 (삭제 이전 기록)

- 앱이 시작되면 OpenDART 상장 종목 목록을 바로 백그라운드에서 준비하고 검색 카드에 준비 상태와 종목 수를 표시한다.
- 첫 다운로드에서 6자리 상장 종목만 추린 경량 TSV 인덱스를 앱 캐시에 만들고, 원본 목록이 갱신되기 전까지 다음 실행에서 재사용한다.
- KIS 수정주가 일봉의 시가·고가·저가·종가·거래량을 보존한다.
- 기술 상태는 최대 100거래일로 MA5·MA20·MA60, RSI(14), MACD(12·26·9), 볼린저밴드(20·2σ), 최근 20일 연환산 변동성과 거래량 비율을 계산한다.
- 지지·저항은 최근 60거래일의 국소 고가·저가, 최근 20일 고가·저가, 이동평균과 볼린저밴드 값을 1.8% 이내에서 군집화한 뒤 현재가 아래·위의 가까운 구간을 각각 최대 2개 표시한다.
- 종합 참고가는 단순 PER과 S-RIM이 모두 있으면 하단·기준·상단 시나리오를 각각 50% 동일 가중하고, 한 방법만 있으면 해당 방법만 사용한다.
- 최근 실적은 매출·영업이익·순이익의 시작 연도 대비 5% 초과 개선·약화를 비교한다.
- 종합 상태는 종합 참고가와 현재가의 차이, 기술 방향, 실적 추세를 -5~+5 규칙 점수로 합성한다.
- 결론과 조건별 관찰 기준은 고정된 규칙과 사용자 문구로 만들며 GPT API를 사용하지 않는다.
- 종합 참고가, 기술 상태와 지지·저항은 미래 가격 예측이나 투자 권유가 아닌 후행 참고값으로 표시한다.
