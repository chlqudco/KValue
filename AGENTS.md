# K-Value 저장소 작업 지침

## 제품 기준

- K-Value는 6자리 한국 주식 종목코드로 가격, 최근 100거래일 차트, 핵심 재무정보, 최근 3년 실적, 단순 PER 참고가, DART 공시 경로를 한 화면에서 제공하는 Android 앱이다.
- 제품 기준 문서는 `C:\Users\kbsmedia\Desktop\K-Value_기획문서_패키지_v1.0`의 PRD, 요구사항·유스케이스, 화면정의서, 기술·API 설계서, MVP 개발·테스트 계획서다.
- 문서와 구현이 충돌하면 요구사항 ID, 계산식, 사용자 문구, 테스트케이스를 함께 확인하고 변경 의도를 기록한다.
- 화면과 코드에서 가치평가 결과를 `적정주가`나 예측값이라고 부르지 않는다. 반드시 `단순 PER 참고가` 또는 `참고가`로 표현한다.
- 결과는 투자 권유가 아니며, 데이터 출처·기준일·계산 가정·면책 문구를 숨기지 않는다.

## MVP 범위

- Android Studio, Kotlin, Jetpack Compose, Material 3 기반의 단일 Activity·단일 세로 스크롤 화면을 유지한다.
- 숫자 6자리 종목코드 조회, 현재가와 등락률, 최대 100거래일 수정주가 종가 차트, EPS/PER/PBR/BPS/ROE, 최대 3개 연도 실적, 목표 PER 편집, DART 외부 브라우저 연결을 구현한다.
- 일반 흑자 보통주를 초기 평가 대상으로 삼는다. EPS가 없거나 0 이하인 기업과 ETF/ETN, 우선주, 스팩, 리츠, 은행·보험·증권 등은 참고가 계산 대상에서 제외하고 이유를 표시한다.
- AI, 로그인, 계좌 연동, 주문, 자동매매, 뉴스, 알림, 실시간 웹소켓, 커뮤니티, 결제는 MVP에 추가하지 않는다.

## 아키텍처

- 패키지는 `ui`, `domain`, `data`, `common` 경계를 유지하고 불필요한 멀티 모듈이나 복잡한 계층을 만들지 않는다.
- UI는 상태를 렌더링하고 사용자 이벤트만 전달한다. Repository 또는 외부 API를 Composable에서 직접 호출하지 않는다.
- ViewModel은 StateFlow 기반 단방향 상태 흐름, 요청 취소, 최신 결과 반영, 입력 검증과 화면 이벤트 조정을 담당한다.
- Repository는 외부 DTO를 도메인 모델로 정규화하고 필수 데이터 실패와 부분 실패를 구분한다. 외부 DTO를 UI로 전달하지 않는다.
- PER 계산, 종목코드 검증, 숫자 포매팅처럼 독립 가능한 로직은 Android 프레임워크에 의존하지 않는 코드로 둔다.
- 실제 API가 연결되기 전에도 샘플 Repository로 전체 화면과 계산 흐름을 실행할 수 있게 유지한다.

## 데이터와 계산 규칙

- 목표 PER 기본값은 보수 10배, 기준 15배, 낙관 20배다.
- 모든 배수는 유한한 0 초과 값이어야 하고 `보수 ≤ 기준 ≤ 낙관` 순서를 만족해야 한다. 잘못된 편집 중에는 이전 정상 계산 결과를 유지한다.
- 참고가는 `EPS × 목표 PER`로 계산하고 100원 단위로 반올림한다.
- 기준 참고가 차이율은 `(기준 참고가 / 현재가 - 1) × 100`으로 계산한다. 현재가가 0 이하이면 계산하지 않는다.
- 누락 데이터를 0으로 대체하거나 임의로 추정하지 않는다. `데이터 없음` 또는 구체적인 계산 불가 사유를 표시한다.
- 가격·차트·재무 데이터는 단위와 서로 독립적인 기준시각 또는 기준기간을 포함한다.
- 일봉은 날짜 오름차순으로 정렬하고 중복 날짜를 제거한다. 2개 미만이면 차트 대신 빈 상태를 표시한다.

## UI와 접근성

- `Idle`, `Loading`, `Success`, `Unsupported`, `Error` 상태를 빈 영역 없이 명확히 표현한다.
- Loading 중 중복 조회를 막고, 새 조회는 이전 요청을 취소하거나 최신 요청 결과만 반영한다. 새로고침은 캐시를 우회한다.
- 상승·하락은 색상에만 의존하지 않고 부호와 문구를 함께 표시한다.
- 텍스트 확대와 좁은 화면에서 입력, 지표, 실적, 참고가 요소가 잘리면 줄바꿈 또는 세로 배치로 전환한다.
- 클릭 가능한 요소에는 화면 텍스트 또는 의미 있는 접근성 라벨을 제공한다. 차트에는 최소·최대값과 기간 변화 요약을 제공한다.
- 사용자에게 보이는 문자열은 문자열 리소스에 둔다. 핵심 컨트롤에는 안정적인 `testTag`를 사용한다.
- DART는 WebView가 아니라 외부 브라우저로 열고, 처리 가능한 앱이 없으면 오류를 안내한다.

## 보안과 외부 API

- App Key, App Secret, 접근토큰, 계좌정보를 소스, 리소스, 로그, 테스트 픽스처에 커밋하지 않는다.
- 개인 POC의 비밀값은 Git에서 제외된 `local.properties`를 통해 주입할 수 있지만 APK에서 보호되지 않는다는 한계를 유지한다.
- 공개 배포 빌드에는 개인 App Secret이나 직접 API 호출용 비밀값을 포함하지 않는다. 공개 서비스 전에는 백엔드 프록시, 키 회전, 시세 이용조건, 보안과 규제를 별도로 검토한다.
- 로그에는 비밀값을 남기지 않고 공급자, 호출 단계, 지연시간, 오류 범주처럼 진단에 필요한 최소 정보만 사용한다.

## 코드 규칙

- Kotlin 공식 스타일과 기존 `com.chlqudco.kvalue` 패키지를 사용한다.
- 새 코드 주석과 KDoc을 작성하지 않는다. 사용자가 명시적으로 요청한 경우에만 필요한 범위로 추가한다.
- 기존 주석도 코드 변경에 필요한 경우에만 최소한으로 수정한다.
- Composable은 상태를 명시적으로 받고 이벤트 콜백을 반환하는 형태를 우선한다.
- 도메인 모델에는 표시용 가공 문자열보다 원본 단위의 타입을 보존하고, 포매팅은 UI 경계에서 수행한다.
- 예외를 사용자 문구로 직접 노출하지 않고 정의된 오류 범주로 변환한다.

## 검증과 완료 기준

- 로컬 단위 테스트는 `gradlew.bat testDebugUnitTest`, Debug 빌드는 `gradlew.bat assembleDebug`로 검증한다.
- 계산기와 검증기는 정상값, 경계값, 0·음수·NaN·무한대, PER 순서 오류, 100원 반올림을 테스트한다.
- 상태 로직은 잘못된 입력의 API 호출 0회, 중복 요청 방지, 최신 요청 우선, 재시도와 새로고침을 검증한다.
- UI는 입력, 조회, 전체 결과, 부분 데이터, 미지원, 오류, DART, 출처와 면책을 검증한다.
- 기능 완료 시 관련 요구사항과 TC-001~TC-036의 기대 결과를 확인한다. P0/P1 결함, 계산 오류, 앱 종료, 비밀정보 노출이 남아 있으면 완료로 보지 않는다.
